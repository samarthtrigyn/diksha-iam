info:
  name: Get SSO Providers
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/iam/sso"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get list of configured SSO providers with login URLs.
