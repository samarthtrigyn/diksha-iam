info:
  name: Get User
  type: http
  seq: 2

http:
  method: GET
  url: "{{iamCoreUrl}}/users/{{userId}}"
  headers:
    - name: Authorization
      value: Bearer {{adminToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get user details by ID. Admin operation.
